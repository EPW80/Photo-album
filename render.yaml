services:
  - type: web
    name: photo-album
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm start
    healthCheckPath: /photos
    envVars:
      - key: NODE_ENV
        value: production
